// Skema konfigurasi kosong untuk tugas 10 — belum ada pencarian, hanya
// membuktikan rantai build -> daftar -> render. Diisi di tugas 11+.
export const configSchema = {};

export type Config = Record<string, never>;
